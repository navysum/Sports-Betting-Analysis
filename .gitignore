# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# Environment
.env
backend/.env

# ML models — commit trained models so they're baked into the Docker image
# Uncomment below to exclude models (if using a retrain-on-deploy strategy instead)
# backend/ml/*.joblib
backend/ml/training_log.json

# Database — runtime only, not committed
*.db

# Data — commit ELO + Dixon-Coles params (small JSON), exclude large/ephemeral files
backend/data/csv/
backend/data/training_cache.npz
backend/data/soccerbet.db
backend/data/api_cache/

# Node
node_modules/
frontend/node_modules/
frontend/dist/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Logs
*.log
training.log

# Accidentally included local paths
OneDrive/
